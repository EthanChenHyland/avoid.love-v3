# avoid.love V2

This repository is **V2 of avoid.love** — an interactive, browser-native love story about falling in love, trying to avoid it, and failing anyway.

Built with Next.js, TypeScript, GSAP, and Lenis.

## Run locally

```bash
npm install
npm run dev
```

For a production build:

```bash
npm run build
npm start
```
